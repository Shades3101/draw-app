import getUser from "@/lib/authGuard"

export default async function Workspace () {

    // const user = await getUser();

    // if(!user) {
    //     return
    // }

    return <div>

    </div>
}